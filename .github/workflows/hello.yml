
name: hello

on:
  push:
    branches:
      - main

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Echo the message
        run: | 
          echo "Hello world!"
      - name: La fecha del intento 2 es
        run: date
      - run: ls -l

    
